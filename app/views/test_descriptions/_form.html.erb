<%= form_for(@test_description) do |f| %>
  <% if @test_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_description.errors.count, "error") %> prohibited this test from being saved:</h2>

      <ul>
      <% @test_description.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :description %><br />
    <%= f.text_area :description, :class => 'span12' %>
  </div>
  <div class="form-actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
