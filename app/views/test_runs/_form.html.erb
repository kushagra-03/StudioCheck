<%= form_for(@test_run) do |f| %>
  <% if @test_run.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_run.errors.count, "error") %> prohibited this test_run from being saved:</h2>

      <ul>
      <% @test_run.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :server_id %><br />
    <%= f.collection_select(:server_id, Server.all, :id, :api_url, :prompt => true) %>
  </div>
  <div class="field">
    <%= f.label :test_description_id %><br />
    <%= f.collection_select(:test_description_id, TestDescription.all, :id, :summary, :prompt => true) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
